import { Button } from "@/components/ui/button";

export default function NavBar() {
  return <header>
    this will be a nav bar
    <Button variant="ghost">Accueil</Button>
    <Button variant="ghost">Accueil</Button>
    <Button variant="ghost">Mon Pokédex</Button>
    </header>
  ;
}
